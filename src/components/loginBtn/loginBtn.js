import React from "react";
import { Button } from "./styled";

const LoginButton = () => {
  return <Button>Login</Button>;
  // later add here logic for redirection to login page
};

export default LoginButton;
